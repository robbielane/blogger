<h1> Articles Tagged with <strong>'<%= @tag.name %>'</strong></h1>

<div class="articles">
  <% @tag.articles.each do |article| %>
    <h4> <%= link_to article.title, article_path(article) %> </h4>
      <p> <%= article.body[0..250]  %>... </p>
  <% end %>
</div>
